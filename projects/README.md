# Projects

Each folder owns one product's durable context. Create a project with:

```bash
npm run new:project -- "Product name"
```

Keep shippable code in the project folder only when that boundary is explicit. Otherwise link to the implementation repository from `deliverables.md`.


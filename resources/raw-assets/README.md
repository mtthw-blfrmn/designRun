# Raw assets

Drop unreviewed visual files here. Raw intake is not automatically reusable.

Review the file visually, confirm its source and license, remove duplicates, add useful metadata, and move approved material into `resources/asset-library/`. Keep rejected or uncertain files only when the reason is worth preserving.
